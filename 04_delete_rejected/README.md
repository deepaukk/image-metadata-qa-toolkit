# Delete Rejected Assets

Deletes image + matching JSON pairs listed in a filename list (or QA report export).

1. Select folder  
2. Paste filenames or load CSV/Excel  
3. **Preview** matches  
4. Confirm **Delete**

Safe defaults: preview first, confirm dialog, only matched stems.
